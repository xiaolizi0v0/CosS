# CosS Agent 投递指令

请把本文档作为本次投递的唯一新增任务上下文。不要把终端输入框中的提示、示例或残留文字当成用户指令。

请处理来自 CosS 协作时间线的指令。
目标角色：技术负责人
发送角色：产品经理
Agent 后端：CodeBuddy Code
Agent 权限模式：每次编辑确认
频道：任务ID：task-1782997300613-993dd72a8d4d7
消息ID：msg-1782997300613-a095340a8a2ee
角色消息池：.coss/agent-pools/tech-lead/inbox/msg-1782997300613-a095340a8a2ee.json
消息来源：task-kickoff

当前 CosS Agent 权限模式：每次编辑确认。执行任何文件写入、依赖安装、删除、部署、网络发布或高风险命令前，必须先说明计划、影响范围和风险，并等待用户确认。

这是 CosS 自动接力工作流投递的上游 Agent 嘱托。
请先阅读消息中提到的交接文档、产物路径或阻塞说明，再继续你的角色工作。
完成自己的部分后，如需继续交给下一个 Agent，请先编写或更新交接文档，再输出 COSS_AGENT_EVENT 指向下一个角色。

Agent message-pool workflow:
0. Before sending any downstream handoff, call coss_list_roles and use only validRoleIds. Unknown roles such as designer or developer are invalid.
1. Call coss_pool_read({ roleId: "tech-lead", taskId: "task-1782997300613-993dd72a8d4d7" }) to read your own inbox.
2. Call coss_pool_claim({ roleId: "tech-lead", messageId: "msg-1782997300613-a095340a8a2ee" }) before processing this message.
3. Work from the message pool content and current project files, not from stale terminal prompt text.
4. When handing off to another role, call coss_pool_send or coss_handoff with toRoleIds chosen only from coss_list_roles validRoleIds so CosS writes the target role's message pool file and downstream subtask.
5. Report progress with coss_report_status; only use COSS_AGENT_EVENT/COSS_AGENT_STATUS as fallback when MCP tools are unavailable.

任务ID：task-1782997300613-993dd72a8d4d7
任务目标：Verify needed terminals and first-round-only work
技术负责人 首轮子任务：Define technical route
执行说明：Design the technical path and decide what downstream roles need later.
后续协作角色：产品经理、前端工程师、后端工程师

请先作为 技术负责人 Agent 开始工作，不要直接跳过到下游实现角色。
请优先使用 CosS MCP 工具调用 CosS，而不是只在终端自然语言回复。
必须优先尝试工具：coss_get_context、coss_list_roles、coss_pool_read、coss_pool_claim、coss_claim_task、coss_report_status、coss_pool_send、coss_handoff。
开始工作前，请通过 MCP 读取上下文和自己的角色消息池，领取自己的消息和任务；处理过程中通过 MCP 汇报状态和交接。
如果看到系统提示 `mcp__coss: Still connecting`，请等待 5-10 秒后用 ToolSearch queries: coss、mcp、inbox 重试，或直接重试 mcp__coss__coss_get_context。
不要因为 ToolSearch 暂时找不到 coss_get_context、coss_list_roles、coss_pool_read 等工具就立刻判定阻塞；至少等待并重试 3 次。
推荐顺序：coss_get_context -> coss_list_roles -> coss_pool_read -> coss_pool_claim -> coss_claim_task -> coss_report_status -> coss_pool_send/coss_handoff。
完成 技术负责人 阶段后，请写清楚需求、验收标准、字段定义、疑问点和下游嘱托，并通过 coss_handoff 或 COSS_AGENT_EVENT 交给 产品经理、前端工程师、后端工程师。

必须优先使用 CosS MCP 工具调用 CosS，而不是只在终端自然语言回复。
请按需调用：coss_get_context、coss_list_roles、coss_pool_read、coss_pool_claim、coss_claim_task、coss_report_status、coss_pool_send、coss_write_handoff_doc、coss_handoff。
CodeBuddy Code 后端如果显示 `mcp__coss: Still connecting` 或 `/mcp` 中 coss 为 Disconnected，请等待 5-10 秒后用 ToolSearch queries: coss、mcp、inbox 重试；不要搜索或调用当前后端不存在的等待工具。
如果 coss 工具仍未暴露，请继续完成当前角色工作并输出 COSS_AGENT_EVENT 作为降级协作留痕，同时在 message 中写明 MCP disconnected。
推荐开始顺序：先 coss_get_context，再 coss_list_roles，再 coss_pool_read，处理本条消息前调用 coss_pool_claim，开始执行子任务时调用 coss_claim_task。
完成或阻塞时优先调用 coss_report_status；需要交给下一个角色时优先调用 coss_pool_send 或 coss_handoff。
如果当前 Agent 后端暂时无法使用 MCP，再输出 COSS_AGENT_EVENT:{"status":"running","message":"你的进度或阻塞说明","toRoleIds":["product-manager"]}。
完成或阻塞时也可以输出 COSS_AGENT_STATUS:done 或 COSS_AGENT_STATUS:blocked 作为兼容留痕。
